{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1 class="my-5">Audit Log</h1>
                <table class="table">
                    <tbody>
                    {% for log in logs %}
                        {{ audit(log) }}
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}
