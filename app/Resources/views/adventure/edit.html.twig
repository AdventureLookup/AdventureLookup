{% extends 'base.html.twig' %}

{% block body %}
    <div class="container" id="page--edit-adventure" data-similar-titles-url="{{ path('similar_titles_search') }}" data-search-url="{{ path('api_autocomplete_field', {field: '__FIELD__'}) }}">
        <div class="row justify-content-center">
            <div class="col-lg-8 col-xl-7">
                <h1 class="my-5">Edit adventure</h1>

                {{ form_start(form) }}

                {% include 'adventure/_form.html.twig' %}

                <input type="submit" value="Save Changes" class="btn btn-primary" role="button" />
                {{ form_end(form) }}
            </div>
        </div>
    </div>
    {% include 'adventure/_new_content_modal.html.twig' %}
{% endblock %}
